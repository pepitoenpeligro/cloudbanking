sudo: false

language: rust

cache: cargo

branches:
  only:
  - master

rust:
  - 1.47.0

#install:
  # - cargo install --force cargo-make

script:
  #cargo make --makefile make.toml test
  # To improve our cache
  cargo test
